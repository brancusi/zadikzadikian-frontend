/** @startingPoint section="Catalog" subtitle="Label/value rows for provenance, dimensions, valuation" viewport="700x320" */
export interface SpecListProps {
  /** Rows in display order */
  items: { label: string; value: string }[];
}
