---
interface Props {
  title?: string;
}

const { title = 'Preview boundary' } = Astro.props;
---
<aside class="editorial-note" aria-label={title}>
  <p class="eyebrow">{title}</p>
  <div class="editorial-note__body">
    <slot />
  </div>
</aside>
